Download all programs into a single folder, then run Setup.py, which creates the folder setup that is used by the programs. After Setup.py has been run, every other program will be found in \Programs.
